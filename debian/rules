#!/usr/bin/make -f

#export DH_VERBOSE = 1


%:
	dh $@

override_dh_auto_configure:
	export CXXFLAGS=-fPIC ; ./configure

# may need to pay attention to ARCH=$(DEB_HOST_MULTIARCH)
override_dh_auto_install:
	$(MAKE) PREFIX=$$(pwd)/debian/cadical/usr install
